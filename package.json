{
  "name": "tiny-pdf-viewer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Build-free, single-page lightweight PDF viewer for the browser",
  "scripts": {
    "test": "node --test"
  }
}